name: TruffleHog Scan

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  trufflehog:
    name: TruffleHog Secret Scan
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Run TruffleHog
      uses: trufflesecurity/trufflehog-actions-scan@v1
      with:
        repo_token: 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
        include_github_actions_secrets: true
